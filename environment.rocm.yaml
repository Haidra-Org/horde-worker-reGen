name: ldm
channels:
  - conda-forge
  - defaults
# These should only contain the minimal essentials to get the binaries going, everything else is managed in requirements.rocm.txt to keep it universal.
dependencies:
  - git
  - pip
  - python==3.11.6
